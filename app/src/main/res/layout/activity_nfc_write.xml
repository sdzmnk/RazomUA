<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#F3F6FF">

    <!-- Іконка NFC -->
    <ImageView
        android:id="@+id/nfcIcon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_nfc"
        android:layout_marginBottom="24dp"
        app:tint="#3A66FF" />

    <!-- Підказка -->
    <TextView
        android:id="@+id/nfcHintText"
        android:text="Піднесіть телефон до NFC-мітки,\nщоб поділитися профілем"
        android:textSize="20sp"
        android:textColor="#1A1A1A"
        android:gravity="center"
        android:layout_marginBottom="32dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- Кнопка -->
    <Button
        android:id="@+id/startWriteButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Почати запис"
        android:textAllCaps="false"
        android:padding="12dp"
        android:backgroundTint="#3A66FF"
        android:textColor="#FFFFFF"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="24dp"/>

</LinearLayout>
